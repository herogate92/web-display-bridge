# 검증 기록 — 2026-09-13

## 개발 버전 추가 검증 — 2026-09-24

- GPU 인코딩 우선: 현재 전송 설정에 대한 `MediaCapabilities.encodingInfo()` 결과로 코덱/프로필 순서를 정하고, 효율이 같으면 H.264를 우선합니다. 이 힌트 자체를 실제 GPU 사용의 증거로 취급하지 않습니다.
- 실제 인코더 이름으로 GPU/CPU/확인 불가를 구분하고, 화면별 CPU 경고를 한국어·영어로 표시합니다. 재연결·종료 시 이전 인코더 표시를 숨깁니다.
- 자동 테스트 19개, TypeScript 검사, 개발 번들 빌드 통과.
- 숨김 Electron 창에서 합성 영상을 PC 내부 WebRTC 연결로 전송했습니다. 1440×1080·60fps 및 1080×810·30fps 설정 모두 `MediaFoundationVideoEncodeAccelerator (NVIDIA H.264 Encoder MFT)`가 실제 보고됐습니다. 첫 설정의 시험 종료 시 전송 크기는 자동 조정된 960×720이었으므로 1440×1080·60fps 성능 달성을 의미하지 않습니다.
- PC 화면에서 CPU 경고 표시, GPU 전환 시 경고 해제, 알 수 없는 인코더, 재연결·종료, 영어 전환을 확인했습니다. 실제 카메라나 모니터 설정 변경 없이 합성 영상과 테스트 상태만 사용했습니다.
- iPad/Safari 및 Android 실기 연결, 동시 두 화면의 GPU 사용과 성능 향상은 아직 검증하지 않았습니다. 설치 파일에는 이번 개발 버전 변경이 아직 포함되지 않았습니다.

아래는 이전 배포 준비 빌드의 검증 기록입니다.

현재 빌드 버전은 `0.2.0-alpha.2`입니다. 자동 검증 결과와 실제 장비에서 확인해야 할 항목을 구분해 기록합니다.

## 자동·빌드 검증

| 항목 | 결과 |
|---|---|
| TypeScript PC·브라우저 코드 검사 | 통과 |
| JavaScript 번들 빌드 | 통과 |
| C# Windows x64 독립 실행형 빌드 | 통과 |
| Win32 구조 크기와 복구 직렬화 자체 검사 | 통과 |
| 연결 번호 만료·일회성 사용·화면별 단일 기기 제한 | 통과 |
| 여러 세션의 독립 연결·종료·재연결 | 통과 |
| 장치 식별 변경·누락·중복·물리 화면 거부 | 통과 |
| HTTP/WS 출처·Host·메시지·요청 횟수 제한 | 통과 |
| 활성 로컬 주 프레임의 화면 캡처 권한 제한 | 통과 |
| 한국어·영어 전환, 정적 문구 누락, PowerShell 한글 인코딩 | 자동 테스트 통과 |
| npm 프로덕션 의존성 보안 감사 | 알려진 취약점 0개 |
| Windows x64 NSIS 설치 파일 생성 | 통과 |

## 실제 장비 검증 현황

이 개발 과정에서 가상 디스플레이 설치, 실제 확장 화면 활성화와 모바일 브라우저 연결까지 사용자 환경에서 진행했습니다. 다음 성능·복구 기준은 아직 통제된 조건에서 완료하지 않았으므로 보장하지 않습니다.

- iPad 9에서 1440×1080·60fps로 10분간 평균 수신 55fps 이상
- 원본 화면과 수신 화면을 함께 촬영한 지연 중앙값 150ms 이하
- iPad, iPhone, Android 기기 2~4대의 동시 독립 연결
- 1시간 연속 사용과 발열 측정
- Wi-Fi 단절, 모바일 잠금, PC 절전 후 재연결
- 강제 종료 후 모든 물리·가상 디스플레이 배치 복구
- 2160×1620·60fps 고화질 모드의 안정성

## 실기 인수 절차

1. Windows와 GPU 드라이버 버전, Virtual Display Driver 버전, 모바일 기기·OS·브라우저, Wi-Fi 조건을 기록합니다.
2. Windows 디스플레이 설정에서 각 가상 화면이 별도 확장 디스플레이로 보이고 서로 다른 창을 표시하는지 확인합니다.
3. 각 가상 화면에서 1440×1080·60fps를 선택하고 서로 다른 모바일 기기를 연결합니다.
4. `/test-pattern`의 움직이는 화면을 10분 전송해 각 수신기의 실제 fps, 손실률과 전송량을 기록합니다.
5. 같은 시간 표시가 보이는 원본과 수신 화면을 함께 촬영해 화면 지연을 계산합니다. 네트워크 RTT를 화면 지연으로 대신하지 않습니다.
6. 전체 연결 종료, 앱 강제 종료, Wi-Fi 단절, 모바일 잠금과 PC 절전을 각각 시험한 뒤 화면과 연결이 복구되는지 확인합니다.
7. 1시간 사용 후 PC와 모바일 기기의 발열, 프레임 저하와 연결 끊김을 기록합니다.

측정 로그는 `node scripts/report.mjs <session.jsonl> [report.json]`으로 요약할 수 있습니다. 로그에는 로컬 IP와 장치 정보가 들어갈 수 있으므로 공개 전에 개인 정보를 지워야 합니다.
